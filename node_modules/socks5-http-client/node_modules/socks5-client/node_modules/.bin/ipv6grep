#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ip-address/bin/ipv6grep.js" "$@"
  ret=$?
else 
  node  "$basedir/../ip-address/bin/ipv6grep.js" "$@"
  ret=$?
fi
exit $ret
